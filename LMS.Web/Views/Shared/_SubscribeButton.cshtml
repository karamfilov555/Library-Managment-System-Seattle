@inject SignInManager<User> SignInManager
@inject UserManager<User> UserManager

@if (!User.IsInRole("Admin") && !User.IsInRole("Member") && !User.IsInRole("Librarian"))
{
    if (!SignInManager.IsSignedIn(User))
    {
        <a class="btn" asp-area="Identity" asp-page="/Account/Login">Enter Now</a>
    }
    else
    {
        //stranichka za plashtane na membership!
        <form method="get">
            <button class="btn btn-primary btn-lg" asp-controller="Membership" asp-action="Subscribe">Get Premium Account</button>
        </form>
    }
}
else
{
    <a class="btn" asp-controller="Book" asp-action="Index">Read Now</a>
}